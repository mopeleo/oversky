<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.oversky.gurms.dao.server.ServerPlayerPackageDao">
    <resultMap id="BaseResultMap" type="org.oversky.gurms.entity.server.ServerPlayerPackage">
        <id column="serverid" jdbcType="VARCHAR" property="serverid" />
        <id column="custno" jdbcType="DECIMAL" property="custno" />
        <id column="itemid" jdbcType="VARCHAR" property="itemid" />
        <result column="itemtype" jdbcType="INTEGER" property="itemtype" />
        <result column="itemnum" jdbcType="INTEGER" property="itemnum" />
    </resultMap>
	
	<sql id="column_list">
        serverid, custno, itemtype, itemid, itemnum
	</sql>
  
    <insert id="insert" parameterType="org.oversky.gurms.entity.server.ServerPlayerPackage">
        insert into server_player_package (serverid, custno, itemtype, itemid, itemnum)
        values (#{serverid, jdbcType=VARCHAR}, #{custno, jdbcType=DECIMAL}, #{itemtype, jdbcType=INTEGER}, #{itemid, jdbcType=VARCHAR}, #{itemnum, jdbcType=INTEGER})
    </insert>
  
    <select id="selectAll" parameterType="org.oversky.gurms.entity.server.ServerPlayerPackage" resultMap="BaseResultMap">
        select <include refid="column_list" />
          from server_player_package
		<where>
			<if test="serverid != null ">
				and serverid = #{serverid, jdbcType=VARCHAR}
			</if>
			<if test="custno != null ">
				and custno = #{custno, jdbcType=DECIMAL}
			</if>
			<if test="itemtype != null ">
				and itemtype = #{itemtype, jdbcType=INTEGER}
			</if>
			<if test="itemid != null ">
				and itemid = #{itemid, jdbcType=VARCHAR}
			</if>
			<if test="itemnum != null ">
				and itemnum = #{itemnum, jdbcType=INTEGER}
			</if>
		</where>
    </select>

	<insert id="insertBatch" parameterType="java.util.List" useGeneratedKeys="false">
		insert all
		<foreach item="item" index="index" collection="list">
		into server_player_package (serverid, custno, itemtype, itemid, itemnum) 
		values (#{item.serverid, jdbcType=VARCHAR}, #{item.custno, jdbcType=DECIMAL}, #{item.itemtype, jdbcType=INTEGER}, #{item.itemid, jdbcType=VARCHAR}, #{item.itemnum, jdbcType=INTEGER})
		</foreach>
		select 1 from dual
	</insert>
	
    <select id="selectById" resultMap="BaseResultMap">
        select <include refid="column_list" />
          from server_player_package
         where 1=1 and serverid = #{0} and custno = #{1} and itemid = #{2}
    </select>

    <delete id="deleteById">
        delete from server_player_package
         where 1=1 and serverid = #{0} and custno = #{1} and itemid = #{2}
    </delete>
  
    <update id="updateById" parameterType="org.oversky.gurms.entity.server.ServerPlayerPackage">
        update server_player_package set 
               itemtype = #{itemtype, jdbcType=INTEGER}, 
               itemnum = #{itemnum, jdbcType=INTEGER}
         where 1=1 and serverid = #{serverid, jdbcType=VARCHAR} and custno = #{custno, jdbcType=DECIMAL} and itemid = #{itemid, jdbcType=VARCHAR}
    </update>

	<update id="dynamicUpdateById" parameterType="org.oversky.gurms.entity.server.ServerPlayerPackage">
		update server_player_package
		<set>
		    <if test="itemtype != null">
				itemtype = #{itemtype, jdbcType=INTEGER},
		    </if>
		    <if test="itemnum != null">
				itemnum = #{itemnum, jdbcType=INTEGER},
		    </if>
		</set>
		where 1=1 and serverid = #{serverid, jdbcType=VARCHAR} and custno = #{custno, jdbcType=DECIMAL} and itemid = #{itemid, jdbcType=VARCHAR}
	</update>
</mapper>