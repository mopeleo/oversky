<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.oversky.gurms.dao.server.ServerPlayerActorFavorDao">
    <resultMap id="BaseResultMap" type="org.oversky.gurms.entity.server.ServerPlayerActorFavor">
        <id column="serverid" jdbcType="VARCHAR" property="serverid" />
        <id column="maleid" jdbcType="VARCHAR" property="maleid" />
        <id column="femaleid" jdbcType="VARCHAR" property="femaleid" />
        <result column="custno" jdbcType="DECIMAL" property="custno" />
        <result column="degree" jdbcType="INTEGER" property="degree" />
    </resultMap>
	
	<sql id="column_list">
        serverid, custno, maleid, femaleid, degree
	</sql>
  
    <insert id="insert" parameterType="org.oversky.gurms.entity.server.ServerPlayerActorFavor">
        insert into server_player_actor_favor (serverid, custno, maleid, femaleid, degree)
        values (#{serverid, jdbcType=VARCHAR}, #{custno, jdbcType=DECIMAL}, #{maleid, jdbcType=VARCHAR}, #{femaleid, jdbcType=VARCHAR}, #{degree, jdbcType=INTEGER})
    </insert>
  
    <select id="selectAll" parameterType="org.oversky.gurms.entity.server.ServerPlayerActorFavor" resultMap="BaseResultMap">
        select <include refid="column_list" />
          from server_player_actor_favor
		<where>
			<if test="serverid != null ">
				and serverid = #{serverid, jdbcType=VARCHAR}
			</if>
			<if test="custno != null ">
				and custno = #{custno, jdbcType=DECIMAL}
			</if>
			<if test="maleid != null ">
				and maleid = #{maleid, jdbcType=VARCHAR}
			</if>
			<if test="femaleid != null ">
				and femaleid = #{femaleid, jdbcType=VARCHAR}
			</if>
			<if test="degree != null ">
				and degree = #{degree, jdbcType=INTEGER}
			</if>
		</where>
    </select>

	<insert id="insertBatch" parameterType="java.util.List" useGeneratedKeys="false">
		insert all
		<foreach item="item" index="index" collection="list">
		into server_player_actor_favor (serverid, custno, maleid, femaleid, degree) 
		values (#{item.serverid, jdbcType=VARCHAR}, #{item.custno, jdbcType=DECIMAL}, #{item.maleid, jdbcType=VARCHAR}, #{item.femaleid, jdbcType=VARCHAR}, #{item.degree, jdbcType=INTEGER})
		</foreach>
		select 1 from dual
	</insert>
	
    <select id="selectById" resultMap="BaseResultMap">
        select <include refid="column_list" />
          from server_player_actor_favor
         where 1=1 and serverid = #{0} and maleid = #{1} and femaleid = #{2}
    </select>

    <delete id="deleteById">
        delete from server_player_actor_favor
         where 1=1 and serverid = #{0} and maleid = #{1} and femaleid = #{2}
    </delete>
  
    <update id="updateById" parameterType="org.oversky.gurms.entity.server.ServerPlayerActorFavor">
        update server_player_actor_favor set 
               custno = #{custno, jdbcType=DECIMAL}, 
               degree = #{degree, jdbcType=INTEGER}
         where 1=1 and serverid = #{serverid, jdbcType=VARCHAR} and maleid = #{maleid, jdbcType=VARCHAR} and femaleid = #{femaleid, jdbcType=VARCHAR}
    </update>

	<update id="dynamicUpdateById" parameterType="org.oversky.gurms.entity.server.ServerPlayerActorFavor">
		update server_player_actor_favor
		<set>
		    <if test="custno != null">
				custno = #{custno, jdbcType=DECIMAL},
		    </if>
		    <if test="degree != null">
				degree = #{degree, jdbcType=INTEGER},
		    </if>
		</set>
		where 1=1 and serverid = #{serverid, jdbcType=VARCHAR} and maleid = #{maleid, jdbcType=VARCHAR} and femaleid = #{femaleid, jdbcType=VARCHAR}
	</update>
</mapper>