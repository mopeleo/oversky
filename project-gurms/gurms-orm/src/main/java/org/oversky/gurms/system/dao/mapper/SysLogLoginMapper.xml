<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.oversky.gurms.system.dao.SysLogLoginDao">
    <resultMap id="BaseResultMap" type="org.oversky.gurms.system.entity.SysLogLogin">
        <id column="logid" jdbcType="DECIMAL" property="logid" />
        <result column="userid" jdbcType="DECIMAL" property="userid" />
        <result column="logindate" jdbcType="VARCHAR" property="logindate" />
        <result column="logintime" jdbcType="VARCHAR" property="logintime" />
        <result column="loginpasswd" jdbcType="VARCHAR" property="loginpasswd" />
        <result column="loginip" jdbcType="VARCHAR" property="loginip" />
        <result column="logintype" jdbcType="INTEGER" property="logintype" />
        <result column="loginresult" jdbcType="INTEGER" property="loginresult" />
    </resultMap>

    <insert id="insert" parameterType="org.oversky.gurms.system.entity.SysLogLogin" useGeneratedKeys="true" keyProperty="logid">
        insert into sys_log_login (userid, logindate, logintime, loginpasswd, loginip, logintype, loginresult)
        values (#{userid, jdbcType=DECIMAL}, #{logindate, jdbcType=VARCHAR}, #{logintime, jdbcType=VARCHAR}, #{loginpasswd, jdbcType=VARCHAR}, #{loginip, jdbcType=VARCHAR}, #{logintype, jdbcType=INTEGER}, #{loginresult, jdbcType=INTEGER})
    </insert>
  
	<!-- mysql专属 -->
	<insert id="insertBatch" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="logid">
		insert into sys_log_login (userid, logindate, logintime, loginpasswd, loginip, logintype, loginresult)
		values 
		<foreach item="item" index="index" collection="list" separator=",">
        (#{item.userid, jdbcType=DECIMAL}, #{item.logindate, jdbcType=VARCHAR}, #{item.logintime, jdbcType=VARCHAR}, #{item.loginpasswd, jdbcType=VARCHAR}, #{item.loginip, jdbcType=VARCHAR}, #{item.logintype, jdbcType=INTEGER}, #{item.loginresult, jdbcType=INTEGER})
		</foreach>
	</insert>
	
    <select id="selectWhere" parameterType="org.oversky.gurms.system.entity.SysLogLogin" resultMap="BaseResultMap">
        select <include refid="column_list"><property name="tab" value=""/></include>
          from sys_log_login 
          <include refid="where_all_list" />
    </select>

    <delete id="deleteWhere" parameterType="org.oversky.gurms.system.entity.SysLogLogin">
        delete from sys_log_login 
          <include refid="where_all_list" />
    </delete>

	<select id="count" resultType="int" parameterType="org.oversky.gurms.system.entity.SysLogLogin">
        select count(1) from sys_log_login <include refid="where_all_list" />
	</select>

    <select id="selectAll" resultMap="BaseResultMap">
        select <include refid="column_list"><property name="tab" value=""/></include>
          from sys_log_login
    </select>

    <select id="getById" resultMap="BaseResultMap">
        select <include refid="column_list"><property name="tab" value=""/></include>
          from sys_log_login
         where logid = #{0}
    </select>

    <delete id="deleteById">
        delete from sys_log_login
         where logid = #{0}
    </delete>
  
    <update id="updateById" parameterType="org.oversky.gurms.system.entity.SysLogLogin">
        update sys_log_login set 
               userid = #{userid, jdbcType=DECIMAL}, 
               logindate = #{logindate, jdbcType=VARCHAR}, 
               logintime = #{logintime, jdbcType=VARCHAR}, 
               loginpasswd = #{loginpasswd, jdbcType=VARCHAR}, 
               loginip = #{loginip, jdbcType=VARCHAR}, 
               logintype = #{logintype, jdbcType=INTEGER}, 
               loginresult = #{loginresult, jdbcType=INTEGER}
		where logid = #{logid, jdbcType=DECIMAL}
    </update>

	<update id="dynamicUpdateById" parameterType="org.oversky.gurms.system.entity.SysLogLogin">
		update sys_log_login
		<set>
		    <if test="userid != null">
				userid = #{userid, jdbcType=DECIMAL},
		    </if>
		    <if test="logindate != null">
				logindate = #{logindate, jdbcType=VARCHAR},
		    </if>
		    <if test="logintime != null">
				logintime = #{logintime, jdbcType=VARCHAR},
		    </if>
		    <if test="loginpasswd != null">
				loginpasswd = #{loginpasswd, jdbcType=VARCHAR},
		    </if>
		    <if test="loginip != null">
				loginip = #{loginip, jdbcType=VARCHAR},
		    </if>
		    <if test="logintype != null">
				logintype = #{logintype, jdbcType=INTEGER},
		    </if>
		    <if test="loginresult != null">
				loginresult = #{loginresult, jdbcType=INTEGER},
		    </if>
		</set>
		where logid = #{logid, jdbcType=DECIMAL}
	</update>

	<update id="updateBatch" parameterType="java.util.List">
	    update sys_log_login
	    <trim prefix="set" suffixOverrides=",">	
	        <trim prefix="userid = case" suffix="end,">
	            <foreach collection="list" item="item" index="index">
	                <if test="item.userid != null">
	                    when logid = #{item.logid} then #{item.userid, jdbcType=DECIMAL}
	                </if>
	                <if test="item.userid == null">
	                    when logid = #{item.logid} then userid
	                </if>
	            </foreach>
	        </trim>
	        <trim prefix="logindate = case" suffix="end,">
	            <foreach collection="list" item="item" index="index">
	                <if test="item.logindate != null">
	                    when logid = #{item.logid} then #{item.logindate, jdbcType=VARCHAR}
	                </if>
	                <if test="item.logindate == null">
	                    when logid = #{item.logid} then logindate
	                </if>
	            </foreach>
	        </trim>
	        <trim prefix="logintime = case" suffix="end,">
	            <foreach collection="list" item="item" index="index">
	                <if test="item.logintime != null">
	                    when logid = #{item.logid} then #{item.logintime, jdbcType=VARCHAR}
	                </if>
	                <if test="item.logintime == null">
	                    when logid = #{item.logid} then logintime
	                </if>
	            </foreach>
	        </trim>
	        <trim prefix="loginpasswd = case" suffix="end,">
	            <foreach collection="list" item="item" index="index">
	                <if test="item.loginpasswd != null">
	                    when logid = #{item.logid} then #{item.loginpasswd, jdbcType=VARCHAR}
	                </if>
	                <if test="item.loginpasswd == null">
	                    when logid = #{item.logid} then loginpasswd
	                </if>
	            </foreach>
	        </trim>
	        <trim prefix="loginip = case" suffix="end,">
	            <foreach collection="list" item="item" index="index">
	                <if test="item.loginip != null">
	                    when logid = #{item.logid} then #{item.loginip, jdbcType=VARCHAR}
	                </if>
	                <if test="item.loginip == null">
	                    when logid = #{item.logid} then loginip
	                </if>
	            </foreach>
	        </trim>
	        <trim prefix="logintype = case" suffix="end,">
	            <foreach collection="list" item="item" index="index">
	                <if test="item.logintype != null">
	                    when logid = #{item.logid} then #{item.logintype, jdbcType=INTEGER}
	                </if>
	                <if test="item.logintype == null">
	                    when logid = #{item.logid} then logintype
	                </if>
	            </foreach>
	        </trim>
	        <trim prefix="loginresult = case" suffix="end,">
	            <foreach collection="list" item="item" index="index">
	                <if test="item.loginresult != null">
	                    when logid = #{item.logid} then #{item.loginresult, jdbcType=INTEGER}
	                </if>
	                <if test="item.loginresult == null">
	                    when logid = #{item.logid} then loginresult
	                </if>
	            </foreach>
	        </trim>
	    </trim>
	    where logid in
	    <foreach collection="list" index="index" item="item" separator="," open="(" close=")">
	        #{item.logid}
	    </foreach>
	</update>
	
	<delete id="deleteBatch">
	    delete from sys_log_login
	     where logid in
	    <foreach collection="array" item="id" separator="," open="(" close=")">
	        #{id}
	    </foreach>
	</delete>

	<sql id="column_list">
        ${tab}logid, ${tab}userid, ${tab}logindate, ${tab}logintime, ${tab}loginpasswd, ${tab}loginip, ${tab}logintype, ${tab}loginresult
	</sql>
	
	<sql id="where_all_list">
		<where>
			<if test="logid != null">
				and logid = #{logid, jdbcType=DECIMAL}
			</if>
			<if test="userid != null">
				and userid = #{userid, jdbcType=DECIMAL}
			</if>
			<if test="logindate != null and logindate != ''">
				and logindate = #{logindate, jdbcType=VARCHAR}
			</if>
			<if test="logintime != null and logintime != ''">
				and logintime = #{logintime, jdbcType=VARCHAR}
			</if>
			<if test="loginpasswd != null and loginpasswd != ''">
				and loginpasswd = #{loginpasswd, jdbcType=VARCHAR}
			</if>
			<if test="loginip != null and loginip != ''">
				and loginip = #{loginip, jdbcType=VARCHAR}
			</if>
			<if test="logintype != null">
				and logintype = #{logintype, jdbcType=INTEGER}
			</if>
			<if test="loginresult != null">
				and loginresult = #{loginresult, jdbcType=INTEGER}
			</if>
		</where>
	</sql>
</mapper>